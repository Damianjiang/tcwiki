# 服务器重启更新指令

## 指令说明

**指令：** `/tcraft gx`

**功能：** 服务器重启更新，5秒倒计时后执行 Paper 服务端重启指令

## 权限要求

只有 OP 或拥有 `tcraft.admin` 权限的玩家可以使用

## 执行流程

① 检查权限和是否已在重启中

② 开始 5 秒倒计时

③ 每秒向所有在线玩家发送倒计时消息

④ 倒计时结束后执行 `restart` 命令